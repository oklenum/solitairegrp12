export function cardFormatter(card) {
  if (card === "AC") {
    const Card = { suit: "C", value: 1 };
    return Card;
  }
  if (card === "2C") {
    const Card = { suit: "C", value: 2 };
    return Card;
  }
  if (card === "3C") {
    const Card = { suit: "C", value: 3 };
    return Card;
  }
  if (card === "4C") {
    const Card = { suit: "C", value: 4 };
    return Card;
  }
  if (card === "5C") {
    const Card = { suit: "C", value: 5 };
    return Card;
  }
  if (card === "6C") {
    const Card = { suit: "C", value: 6 };
    return Card;
  }
  if (card === "7C") {
    const Card = { suit: "C", value: 7 };
    return Card;
  }
  if (card === "8C") {
    const Card = { suit: "C", value: 8 };
    return Card;
  }
  if (card === "9C") {
    const Card = { suit: "C", value: 9 };
    return Card;
  }
  if (card === "10C") {
    const Card = { suit: "C", value: 10 };
    return Card;
  }
  if (card === "JC") {
    const Card = { suit: "C", value: 11 };
    return Card;
  }
  if (card === "QC") {
    const Card = { suit: "C", value: 12 };
    return Card;
  }
  if (card === "KC") {
    const Card = { suit: "C", value: 13 };
    return Card;
  }
  if (card === "AD") {
    const Card = { suit: "D", value: 1 };
    return Card;
  }
  if (card === "2D") {
    const Card = { suit: "D", value: 2 };
    return Card;
  }
  if (card === "3D") {
    const Card = { suit: "D", value: 3 };
    return Card;
  }
  if (card === "4D") {
    const Card = { suit: "D", value: 4 };
    return Card;
  }
  if (card === "5D") {
    const Card = { suit: "D", value: 5 };
    return Card;
  }
  if (card === "6D") {
    const Card = { suit: "D", value: 6 };
    return Card;
  }
  if (card === "7D") {
    const Card = { suit: "D", value: 7 };
    return Card;
  }
  if (card === "8D") {
    const Card = { suit: "D", value: 8 };
    return Card;
  }
  if (card === "9D") {
    const Card = { suit: "D", value: 9 };
    return Card;
  }
  if (card === "10D") {
    const Card = { suit: "D", value: 10 };
    return Card;
  }
  if (card === "JD") {
    const Card = { suit: "D", value: 11 };
    return Card;
  }
  if (card === "QD") {
    const Card = { suit: "D", value: 12 };
    return Card;
  }
  if (card === "KD") {
    const Card = { suit: "D", value: 13 };
    return Card;
  }
  if (card === "AH") {
    const Card = { suit: "H", value: 1 };
    return Card;
  }
  if (card === "2H") {
    const Card = { suit: "H", value: 2 };
    return Card;
  }
  if (card === "3H") {
    const Card = { suit: "H", value: 3 };
    return Card;
  }
  if (card === "4H") {
    const Card = { suit: "H", value: 4 };
    return Card;
  }
  if (card === "5H") {
    const Card = { suit: "H", value: 5 };
    return Card;
  }
  if (card === "6H") {
    const Card = { suit: "H", value: 6 };
    return Card;
  }
  if (card === "7H") {
    const Card = { suit: "H", value: 7 };
    return Card;
  }
  if (card === "8H") {
    const Card = { suit: "H", value: 8 };
    return Card;
  }
  if (card === "9H") {
    const Card = { suit: "H", value: 9 };
    return Card;
  }
  if (card === "10H") {
    const Card = { suit: "H", value: 10 };
    return Card;
  }
  if (card === "JH") {
    const Card = { suit: "H", value: 11 };
    return Card;
  }
  if (card === "QH") {
    const Card = { suit: "H", value: 12 };
    return Card;
  }
  if (card === "KH") {
    const Card = { suit: "H", value: 13 };
    return Card;
  }
  if (card === "AS") {
    const Card = { suit: "S", value: 1 };
    return Card;
  }
  if (card === "2S") {
    const Card = { suit: "S", value: 2 };
    return Card;
  }
  if (card === "3S") {
    const Card = { suit: "S", value: 3 };
    return Card;
  }
  if (card === "4S") {
    const Card = { suit: "S", value: 4 };
    return Card;
  }
  if (card === "5S") {
    const Card = { suit: "S", value: 5 };
    return Card;
  }
  if (card === "6S") {
    const Card = { suit: "S", value: 6 };
    return Card;
  }
  if (card === "7S") {
    const Card = { suit: "S", value: 7 };
    return Card;
  }
  if (card === "8S") {
    const Card = { suit: "S", value: 8 };
    return Card;
  }
  if (card === "9S") {
    const Card = { suit: "S", value: 9 };
    return Card;
  }
  if (card === "10S") {
    const Card = { suit: "S", value: 10 };
    return Card;
  }
  if (card === "JS") {
    const Card = { suit: "S", value: 11 };
    return Card;
  }
  if (card === "QS") {
    const Card = { suit: "S", value: 12 };
    return Card;
  }
  if (card === "KS") {
    const Card = { suit: "S", value: 13 };
    return Card;
  }
}
